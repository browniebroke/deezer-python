name: Upgrader

on:
  workflow_dispatch:
  schedule:
    - cron: "26 21 10 * *"

jobs:
  upgrade:
    permissions:
      contents: write
      pull-requests: write
    uses: browniebroke/github-actions/.github/workflows/poetry-upgrade.yml@v1
    secrets:
      gh_pat: ${{ secrets.GITHUB_TOKEN }}
